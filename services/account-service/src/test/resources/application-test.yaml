spring:
  datasource:
    url: jdbc:h2:mem:account_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false

auth:
  jwt:
    privateKeyPath: /Users/diwakarbhatt/Projects/Job&InteviewCoPilot/secrets/jwt-keystore.p12
    privateKeyAlias: jwt-signing-key
    privateKeyPassword: changeit
    issuer: account-service
    expirationSeconds: 3600
